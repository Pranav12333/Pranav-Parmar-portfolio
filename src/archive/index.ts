// src/archive/index.ts
//
// Removed code and superseded assets, retained for provenance. Nothing in here is
// referenced by the module graph; the media under ./media/superseded/ is likewise
// unreferenced, so the bundler never emits it.

export { useParallax } from "./deprecated/use-parallax";
